# GitHub token for API requests (fetch-repos.sh)
# Unauthenticated: 60 req/hr limit. Set to increase limit.
# GITHUB_TOKEN=ghp_xxx

# Used by next.config.ts for basePath in CI builds
# GITHUB_ACTIONS=true
